// test-code.js - This triggers your detection logic

const compliantArray = [1, 2, 3];
// This feature ('array-flat') will be detected by your plugin
const nonCompliantResult = compliantArray.flat(1);